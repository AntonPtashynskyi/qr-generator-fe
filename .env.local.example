# ==============================================================================
# Frontend - Local Development Environment (WITHOUT Docker)
# ==============================================================================
# This file is used when running frontend locally: npm run dev
# Copy this file to .env.local and adjust values for your local setup
# ==============================================================================

# Backend API URL (server-side, not exposed to browser)
BACKEND_API_URL=http://localhost:3000

# Public URLs (exposed to browser via NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_BASE_URL=http://localhost:3003
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:3004
